# POS Frontend Docs

See `frontend/pos/README.md` for setup/run/env instructions.
See `docs/pos/ARCHITECTURE.md` for architecture and the day-to-day programmer guide (folded into
one file for the POS app, since it has four small features rather than Inventory's two — the
per-feature responsibility table in ARCHITECTURE.md serves as the programmer guide).
