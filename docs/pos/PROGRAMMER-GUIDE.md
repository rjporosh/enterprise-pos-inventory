# POS Frontend — Programmer Guide

Folded into `docs/pos/ARCHITECTURE.md` (see the "Feature responsibilities" table and the
per-flow sections). Naming conventions, error normalization, and typed-hooks patterns are
identical to the Inventory app — see `docs/inventory/PROGRAMMER-GUIDE.md`, which applies here
unchanged.
